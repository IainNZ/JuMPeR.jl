To build docs, make sure you have a Python virutalenv active with the
following packages installed:

```
mkdocs
pygments
mkdocs-cinder
python-markdown-math
```

Then run, with `Documenter.jl` installed:

```
julia make.jl
mkdocs build
```
